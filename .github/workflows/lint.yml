name: linter

# Disabled - linting now runs as part of deploy workflows
# on:
#   push:
#     branches:
#       - develop
#       - main
#       - master
#       - production
#       - staging
#       - acceptance
#   pull_request:
#     branches:
#       - develop
#       - main
#       - master
#       - production
#       - staging
#       - acceptance

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  quality:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install Dependencies
        run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

      - name: Install Node Dependencies
        run: pnpm install

      - name: Run Pint
        run: vendor/bin/pint

      - name: Analyze Code (PHPStan)
        run: vendor/bin/phpstan analyse --memory-limit=4G --ansi

      - name: Format Frontend
        run: pnpm run format

      - name: Lint Frontend
        run: pnpm run lint

      # - name: Commit Changes
      #   uses: stefanzweifel/git-auto-commit-action@v5
      #   with:
      #     commit_message: fix code style
      #     commit_options: '--no-verify'
